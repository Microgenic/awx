---
- name: Clean VMB's workspace
  hosts: vmb
  become: true
  become_user: root
  
  tasks:
    - name: Delete workspace
      shell: rm -r ~/agent/workspace/*

  tasks:
    - name: Cleanup docker
      shell: docker system prune -a -f